// Get the length of a string and using double quotes

const fullName = "Alexander Corvinus";
console.log(fullName.length);

// Concatenation of Strings and using single quotes
const firstSon = 'Marcus';
const secondSon = 'Lycan';

console.log(fullName + ' is the father of ' + firstSon + ' and ' + secondSon + '.');