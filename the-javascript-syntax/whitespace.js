const name = 'Immanuel Diai';
const animal='Dog';

// Notice how the spacings between the tokens help for readability compared to the one without spacing.

function divide(numOne, numTwo){
    return numTwo / numOne;
}

function multiply(numOne,numTwo){return numTwo*numOne}; 

// Notice the difference in readability between the two functions above due to usage of whitespace.

// You can see that writing a big program without spacing will be a bad idea